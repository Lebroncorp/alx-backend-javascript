/**
 * displays a msg on stdout
 * @param {String} msg to be displayed
 * @author Kenechukwu Uzodinma <https://github.com/LebronCorp>
 */

const displayMessage= (msg) => {
  console.log(msg);
}

module.exports = displayMessage;
