namespace Subjects {
  export interface Teacher {
    firstName: string; // Property representing the first name of the teacher(required)
    lastName: string; // Property representing the last name of the teacher(required)
  }
}
